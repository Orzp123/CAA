# 数据完整性与边界约束需求质量检查清单：学校租户管理与账户管理

**用途**：验证规格中数据约束与边界条件相关需求的完整性、清晰度与一致性（进入 /speckit-plan 前）
**创建日期**：2026-06-25
**深度**：深度（覆盖所有场景类含恢复/回滚路径）
**功能**: [spec.md](../spec.md)

## 唯一性约束需求

- [ ] CHK074 - School Code 平台内唯一的约束是否定义了大小写敏感性（"TEST" 与 "test" 是否视为相同）？[Clarity, Spec §边界与约束]
- [ ] CHK075 - 账户登录名租户内唯一的约束是否定义了大小写敏感性？[Clarity, Spec §边界与约束]
- [ ] CHK076 - School Code 创建后不可修改的约束是否定义了系统层面的强制方式（接口拒绝 vs 前端禁用）？[Clarity, Spec §边界与约束]
- [ ] CHK077 - 运营位标题是否有唯一性约束需求，还是允许同名多条？[Gap, Ambiguity, Spec §1.1]

## 数量上限约束需求

- [ ] CHK078 - 批量导入单次 1000 条上限是否定义了计算方式（文件总行数 vs 有效数据行数，排除表头）？[Clarity, Spec §边界与约束]
- [ ] CHK079 - 运营位最多 10 条的上限超出时的错误行为（接口拒绝 vs 前端禁用）是否定义？[Clarity, Spec §边界与约束]
- [ ] CHK080 - 权益套餐中 Agent 数量等配额约束的具体数值范围是否需要在规格中定义，还是由套餐数据驱动？[Gap, Assumption, Spec §1.1]
- [ ] CHK081 - 一个学校可创建的账户总数是否有上限需求？[Gap, Completeness]

## 字段格式约束需求

- [ ] CHK082 - 学校域名（可选字段）的格式约束（URL 格式/域名格式/是否含协议头）是否定义？[Clarity, Spec §1.1]
- [ ] CHK083 - 运营位链接字段的格式约束（HTTP/HTTPS 协议、相对路径 vs 绝对路径）是否定义？[Gap, Completeness, Spec §1.1]
- [ ] CHK084 - 账户邮箱、手机号的格式校验规则是否定义？[Gap, Completeness, Spec §2.1]
- [ ] CHK085 - Logo 上传支持 SVG 格式，是否有禁止含脚本 SVG 的安全约束需求？[Gap, Edge Case, Spec §边界与约束]

## 状态转换约束需求

- [ ] CHK086 - 学校状态（正常/停用）的合法转换路径是否定义（停用后可直接再启用）？[Clarity, Spec §1.3]
- [ ] CHK087 - 账户停用时是否影响已登录会话的需求是否定义？[Gap, Completeness, Spec §2.3]
- [ ] CHK088 - 停用学校后该校账户状态是否联动变更（保持启用记录 vs 批量停用）的需求是否定义？[Gap, Ambiguity, Spec §边界与约束]

## 数据删除与保留需求

- [ ] CHK089 - 删除账户后历史操作记录的保留策略是否定义？[Gap, Completeness, Spec §2.4]
- [ ] CHK090 - 停用学校后学校数据的保留策略是否定义（软删除 vs 物理删除）？[Gap, Completeness, Spec §1.3]
- [ ] CHK091 - 批量删除操作本身是否需要审计日志记录的需求是否定义？[Gap, Non-Functional]

## 多租户数据隔离约束需求

- [ ] CHK092 - 学校管理员查询账户列表时租户过滤的约束方式（tenantId 自动注入 vs 显式传参）是否定义？[Clarity, Spec §边界与约束]
- [ ] CHK093 - 运营位展示给学生/教师时只展示本校运营位的隔离需求是否明确定义？[Gap, Completeness, Spec §1.1]
- [ ] CHK094 - 品牌配置（Logo/系统名称）按租户隔离展示的需求是否在规格中明确？[Clarity, Spec §场景1]

## 数据一致性与关联约束需求

- [ ] CHK095 - 权益套餐变更时已关联学校的权限数据一致性需求是否定义？[Gap, Recovery, Spec §假设]
- [ ] CHK096 - 批量修改账户类型时是否有约束（如不允许将最后一个 SCHOOL_ADMIN 降级）的需求定义？[Gap, Edge Case, Spec §2.5]
- [ ] CHK097 - 学校缩写作为唯一标识若被其他实体引用，修改约束是否需要级联说明？[Gap, Dependency, Spec §边界与约束]
